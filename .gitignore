# .gitignore for informal_fxmarket
# Generated: sensible defaults for a Python data science project

# Python cache and compiled files
__pycache__/
*.py[cod]
*$py.class
*.pyo
*.pyd
*.pdb

# Byte-compiled / optimized / DLL files
*.pyc

# Packaging / build
build/
dist/
*.egg-info/
pip-wheel-metadata/
.eggs/
*.egg

# Virtual environments
venv/
ENV/
env/
.env/
.venv/
venv*/

# VS Code workspace & settings
.vscode/
*.code-workspace

# JetBrains IDEs
.idea/
*.iml

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Logs and runtime files
*.log
*.out
*.err
*.pid

# Testing / coverage
.coverage
coverage.xml
htmlcov/
.pytest_cache/

# Static analysis caches
.mypy_cache/
.pytype/
.cython_debug/

# Jupyter Notebook checkpoints
.ipynb_checkpoints/

# If you keep notebooks in repo, do NOT ignore *.ipynb; but ignore transient notebook outputs
# (Note: if you want to ignore all notebooks, uncomment the following line)
# *.ipynb

# Local environment / secrets
.env
.env.*
.secret
secrets.*
credentials.json

# Databases
*.sqlite3
*.db
*.db-journal

# Data files / outputs (adjust as needed)
# Keep data/raw/ in repository if it is source-of-truth, but ignore processed/ and large artifacts
# data/processed/
# results/
# models/
# Plots/

# Large model artifacts
*.h5
*.hdf5
*.ckpt
*.pt
*.pth
*.joblib
*.pkl

# Ignore wheel / egg / compiled distributions
*.whl

# Ignore files generated by CI / containers
*.log
docker-compose.override.yml

# Keep workspace file (optional) if you want to share settings across team
# informal_fxmarket.code-workspace

# Add below any project-specific patterns you want to ignore
# Example: local database for development
# local_dev.db

# Note:
# - This .gitignore will prevent new files from being tracked. To stop tracking files already committed,
#   run (in the repo root):
#       git rm -r --cached data/processed/
#       git rm -r --cached results/
#       git rm -r --cached models/
#       git commit -m "Remove ignored files from tracking"
# - Review the file before committing to ensure you don't ignore things you need in the repo
